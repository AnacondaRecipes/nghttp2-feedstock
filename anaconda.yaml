anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: nghttp2/nghttp2
      use_max_tag: true
      include_regex: ^v\d+\.\d+\.\d+$
      from_pattern: ^v(.+)$
      to_pattern: \1
    packages:
      - libnghttp2
      - libnghttp2-static
